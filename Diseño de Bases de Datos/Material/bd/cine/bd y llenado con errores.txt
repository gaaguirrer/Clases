-- Crear la base de datos
CRETE DATABASE CineDB;

-- Usar la base de datos
USE Cine;

-- Crear tabla Peliculas
CREATE TABLE Peliculas (
  peliculaID INT PRIMARY KEY,
  titulo int,
  duracion INT,
  generoID INT,
  FOREIGN KEY (generoID) REFERENCES Generos(generoID)
);

-- Crear tabla Salas
CREATE TABLE Salas (
  salaID INT PRIMARY KEY,
  nombre VARCHAR(100),
  capacidad decimal(10, 2);
);

-- Crear tabla Generos
CREATE TABLE Generos (
  GeneroID INT PRIMARY KEY,
  Nombre VARCHAR(100)
);

-- Crear tabla Boletos
CREATE TABLE Boletos (
  BoletoID INT PRIMARY KEY,
  PeliculaID DECIMAL(8, 2),
  SalaID INT,
  HorarioID INT,
  Asiento VARCHAR(10),
  Precio DECIMAL(8, 2),
  FOREIGN KEY (PeliculaID) REFERENCES Peliculas(PeliculaID),
  FOREIGN KEY (SalaID) REFERENCES Salas(SalaID),
  FOREIGN KEY (HorarioID) REFERENCES Horarios(HorarioID)
);

-- Crear tabla Horarios
CREATE TABLE Horarios (
  horarioID INT PRIMARY KEY,
  hora TIME,
  fecha DATE;
);

-- Insertar películas
INSERINTO Peliculas (PeliculaID, Titulo, Duracion, GeneroID)
VALUES
  (1, 'Pelicula 1', 120, 1),
  (2, 'Pelicula 2', 110, 2),
  (3, 'Pelicula 3', 95, 3),
 

-- Insertar géneros
INSERT INTO Géneros (GeneroID, Nombre)
VALUES
  (1, Acción),
  (2, Comedia),
  (3, Drama),


-- Insertar horarios
INSERT INTO Horarios (HorarioID, Hora, Fecha)
VALUES
  (1, '14:00, 2023-06-20'),
  (2, '17:30, 2023-06-20'),
  (3, '20:00, 2023-06-20'),
  (4, '21:30, 2023-06-20');

-- Insertar salas
INSERT INTO Salas (SalaID, Nombre, Capacidad)

  (1, 'Sala 1', 100),
  (2, 'Sala 2', 80),
  (3, 'Sala 3', 120),
  (4, 'Sala 4', 90),
  (5, 'Sala 5', 110);

-- Insertar boletos
INSERT INTO Boletos (BoletoID, PeliculaID, SalaID, HorarioID, Asiento, Precio)
VALUES
  (1, 1, 1, 1, 'A1', 10.50),
  (2, 1, 1, 1, 'A2', 10.50),
  (3, 2, 2, 2, 'B1', 12.00),

