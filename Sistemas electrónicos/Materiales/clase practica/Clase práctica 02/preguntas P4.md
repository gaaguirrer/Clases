## 5 preguntas (basadas exclusivamente en el código del PDF)

1. ¿Cuántos colores diferentes se muestran en la secuencia del `loop()`?  
2. ¿Qué color se muestra inmediatamente después del color Verde?  
3. Si se intercambian los valores de `ledG` y `ledB` en la línea `int ledR = 9, ledG = 11, ledB = 10;`, ¿qué color aparecería cuando el código intenta mostrar el color Cian?  
4. ¿Por qué el código no necesita volver a ejecutar `pinMode` para el pin 10 dentro del `loop()`?  
5. ¿Qué valor tiene la variable `espera` y en qué unidad de tiempo se interpreta dentro de `delay()`?

## 5 ejercicios de modificación del código

1. Modifica el código para que el color Blanco dure 2 segundos en lugar de 1 segundo, sin cambiar la duración de los demás colores.  
2. Cambia la secuencia para que los colores se muestren en este orden: Rojo, Verde, Azul (elimina Cian, Magenta, Amarillo y Blanco).  
3. Agrega un nuevo color llamado "Naranja" al final de la secuencia, sabiendo que el naranja se produce encendiendo Rojo y Verde al mismo tiempo (y apagando Azul).  
4. Invierte completamente el orden de la secuencia original (debe empezar con Blanco y terminar con Azul).  
5. Haz que la secuencia completa se ejecute el doble de rápido modificando **una sola línea** del código.